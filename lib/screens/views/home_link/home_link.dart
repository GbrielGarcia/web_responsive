import 'package:flutter/material.dart';

class HomeLinkViews extends StatelessWidget {
  const HomeLinkViews({Key? key}) : super(key: key);

  static const String route = '/url';

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('Prueba'),
      ),
    );
  }
}
