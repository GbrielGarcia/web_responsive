import 'package:flutter/material.dart';

class OptionHorizontal extends StatelessWidget {
  const OptionHorizontal({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      height: 30,
      color: Colors.red,
    );
  }
}
